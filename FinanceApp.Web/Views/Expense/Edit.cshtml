@model FinanceApp.Web.Models.ExpenseEditViewModel

@{
    ViewData["Title"] = "Edit Expense";
}

<h1 class="page-title">Edit Expense</h1>

@* the actual form is pulled from a partial so that when the page is requested via
   AJAX we can render just the markup without the layout/nav/sidebar. *@
@await Html.PartialAsync("_ExpenseEditPartial", Model)

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}