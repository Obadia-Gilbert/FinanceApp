{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_jobsComment": "API owns the background jobs locally too (matches the production default in docker-compose.yml) — running Web and API together no longer double-runs RecurringTransactionJob/DailyActivityReminderJob/ExchangeRateRefreshJob.",
  "Jobs": {
    "Enabled": false
  }
}
