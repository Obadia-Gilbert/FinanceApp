{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_storageComment": "Profile photos are uploaded by the Web app into its own wwwroot. Locally the two projects have separate wwwroot folders, so the API is pointed at Web's to read them; in production docker-compose mounts one shared `uploads` volume into both containers and this override isn't needed.",
  "Storage": {
    "UploadsRoot": "../FinanceApp.Web/wwwroot/uploads"
  }
}
